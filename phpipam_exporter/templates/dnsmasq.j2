# Generated by phpipam-exporter
{% for address in addresses | sort(attribute='ip') -%}
{% if address.mac -%}
dhcp-host={{ address.mac | lower }},{{ address.ip }},{{ address.hostname | lower }}
{% endif -%}
{% endfor %}
