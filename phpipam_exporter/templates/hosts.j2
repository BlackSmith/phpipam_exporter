# Generated by phpipam-exporter
{% for address in addresses | sort(attribute='ip') -%}
{% if address.hostname -%}
{{ address.ip }}  {{ address.hostname | lower }} {{ address.hostname.split('.') | first | lower }}
{% endif -%}
{% endfor %}
